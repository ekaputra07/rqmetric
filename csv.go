package main

import (
  "fmt"
  // "os"
  // "errors"
  // "encoding/csv"
)

func WriteCSV(fileName string, header []string, data [][]interface{}) error {
  //file, err := os.Create(fileName)
  fmt.Println(data)
  return nil
}